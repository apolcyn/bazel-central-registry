matrix:
  platform:
  - ubuntu2004
  - macos
  - windows
tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    build_targets:
    - "//:grpc"
    - "//:grpc++"
    - "//:grpc++_alts"
    - "//:grpc_cli"
    - "//src/proto/grpc/testing:test_service_proto"
